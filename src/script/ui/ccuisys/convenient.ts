import { _decorator } from "cc";

export const ccclass = _decorator.ccclass
export const property = _decorator.property
export const menu = _decorator.menu
export const executeInEditMode = _decorator.executeInEditMode
